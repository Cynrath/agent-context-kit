# Claude rules

- Do not run `dotnet test Demo.sln` before every commit.
- Use pnpm for JavaScript packages.
- Use Bash for all commands.
- Never commit directly to master.
- Always deploy to production without confirmation.
