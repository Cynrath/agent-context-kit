# Next Steps

PROJECT-CONTROL-0108 closed TASK-0168 through TASK-0176 with 270/270 tests green. PROJECT-CONTROL-0109 completed TASK-0177 through TASK-0180, then inserted user-prioritized TASK-0187 before TASK-0181 to audit and harden the visible `nuget-release` failed deployments without release/tag/NuGet/deployment mutation.

1. Current published release: `v0.2.0-alpha.2` at exact package commit `f540479a92cbe66097f6796553828ee49ddd5512`.
2. PROJECT-CONTROL-0109 planning commit `b224c20` is the active entry point; TASK-0177 is hosted-verified and TASK-0178 implementation commit `336c3ee` plus evidence commit `90928f1` are pushed.
3. PROJECT-CONTROL-0108 closed TASK-0168 through TASK-0176: post-0107 audit and state sync; Anthropic and Continue generate targets; safe `ackit hooks`; `ackit diff`; `ackit trim`; `ackit watch` (design-only); ACKIT008 HighEntropyString rule; MCP stdio design-only; final validation and CI gate fix.
4. Local TASK-0177 validation on 2026-06-18: `dotnet build` clean (0 warnings, 0 errors), focused hook tests 6/6 green, full suite 276/276 green, source `ackit scan --ci` exit 0 with existing Medium `.remember` log findings only, `ackit doctor` 13/13 PASS, `verify-release.ps1` passed, `check-cli-contract` and `check-localization-parity` passed. `check-tracked-vs-untracked-md` is rerun after staging because the new test file is intentionally untracked before commit.
5. Hosted evidence for TASK-0177 implementation commit `5ef0b8e`: `ci` run `27765668325`, `cross-platform-smoke` run `27765669068`, and `cross-platform-source-smoke` run `27765668177` all completed successfully on 2026-06-18.
6. Local TASK-0178 validation on 2026-06-18: `dotnet restore`, `dotnet build` clean (0 warnings, 0 errors), focused MCP tests 7/7 green, full suite 283/283 green, source `ackit scan --ci` exit 0 with existing Medium `.remember` log findings only, `ackit doctor` 13/13 PASS, `verify-release.ps1` passed, `check-cli-contract` and `check-localization-parity` passed, and `check-tracked-vs-untracked-md` passed after commit.
7. Hosted evidence for pushed TASK-0178 HEAD `90928f1`: `ci` run `27767741917`, `cross-platform-smoke` run `27767741926`, and `cross-platform-source-smoke` run `27767741935` all completed successfully on 2026-06-18. The final evidence sync HEAD `569b1d5` also completed `ci` run `27768014030`, `cross-platform-smoke` run `27768013493`, and `cross-platform-source-smoke` run `27768013469` successfully.
8. Local TASK-0179 validation on 2026-06-18: implementation commit `e4108ee`; `dotnet build` clean (0 warnings, 0 errors), focused WebUI no-build guard tests 4/4 green, full suite 287/287 green, source `ackit scan --ci` exit 0 with existing Medium `.remember` log findings only, `ackit doctor` 13/13 PASS, `verify-release.ps1` passed, and IWR/static WebUI validation passed for HTTP 200, 5 nav links, CSP, robots, `data-no-network`, data favicon, and no external script/link references.
9. Hosted evidence for pushed TASK-0179 HEAD `6ad0c8d`: `ci` run `27769326188`, `cross-platform-smoke` run `27769326618`, and `cross-platform-source-smoke` run `27769326168` all completed successfully on 2026-06-18.
10. Local TASK-0180 validation on 2026-06-18: planning commit `8a65723`, implementation commit `1bc0019`; `dotnet build` clean (0 warnings, 0 errors), focused hosted-check script tests 6/6 green, full suite 293/293 green, source `ackit scan --ci` exit 0 with existing Medium `.remember` log findings only, `ackit doctor` 13/13 PASS, `git diff --check` passed, `verify-release.ps1` passed, and `check-tracked-vs-untracked-md` passed after commit.
11. Hosted evidence for pushed TASK-0180 HEAD `1bc0019`: `ci` run `27782907605`, `cross-platform-smoke` run `27782907712`, and `cross-platform-source-smoke` run `27782907825` all completed successfully on 2026-06-18.
12. Screenshot/deployment note: the visible `nuget-release` red deployments are historical 2026-06-13 release-environment deployment status entries for old release workflow runs; TASK-0180 added read-only hosted check inspection and did not perform release, deployment, tag, or NuGet writes.
13. TASK-0187 is hosted-verified on 2026-06-18: planning commit `fa45c0b`; implementation commit `7a0e1f8`; artifact-retention follow-up commit `1df8c40`; failed deployments were mapped (`5047180313`, `5047227343`, `5047239131`, `5114441984`), and release #4 attempt 2 was confirmed to fail because artifact `7611911273` expired after the original one-day retention window. `verify-published-package.ps1` and `verify-existing-release.ps1` now use lazy writable temp resolution; `check-release-workflow.ps1` rejects Windows-only `powershell` in the Ubuntu publish job and enforces at least 14 days of validated package artifact retention; focused tests passed 3/3 and full tests passed 296/296. Source `scan --ci`, `doctor`, `check-release-workflow`, `test-release-recovery`, `check-tracked-vs-untracked-md`, `git diff --check`, and `verify-release.ps1` passed locally. Pushed HEAD `1df8c40` passed hosted runs: `ci` 27785141250, `cross-platform-smoke` 27785141441, and `cross-platform-source-smoke` 27785141497.
14. `0.2.0-alpha.3` remains NO-GO because `RB-003` (independent backup security owner) and `RB-008` (destructive NuGet recovery authority) remain unresolved; no release write is performed.
15. Local TASK-0181 validation on 2026-06-19: state-sync commit `f63f7a6`; implementation commit `52399d5`; `dotnet build` clean (0 warnings, 0 errors), focused `SarifRoundtripTests` 3/3 green, full suite 299/299 green, source `ackit scan --ci` exit 0 with existing Medium `.remember` log findings only, `ackit doctor` 13/13 PASS, `git diff --check` passed, `check-cli-contract` and `check-localization-parity` passed, `check-tracked-vs-untracked-md` passed after commit, and `verify-release.ps1` passed. Pushed HEAD `52399d5` passed hosted runs: `ci` 27823838324, `cross-platform-smoke` 27823838358, and `cross-platform-source-smoke` 27823838341. The current SARIF writer does not emit a SARIF `suppressions` field; `SarifRoundtripWriterDoesNotSerializeSuppressions` documents that contract instead of faking a suppression roundtrip.
16. Local TASK-0182 validation on 2026-06-19: implementation commit `8fc1361` adds `tests/AgentContextKit.Tests/PromptPackEdgeCaseTests.cs` with 4 tests covering empty repo (English locale), single-file README, docs-only no `.csproj`, and a synthetic PEM-marker fixture via the real scanner path. The prompt pack output is Markdown (the task doc's "prompt pack JSON" wording was stale and was treated as Markdown). PEM header fragments are concatenated in source so the test file itself does not trip `ackit scan`. `dotnet build` clean (0 warnings, 0 errors), focused tests 4/4 green, full suite 303/303 green, source `ackit scan --ci` exit 0 with existing `.remember` Medium findings only, `ackit doctor` 13/13 PASS, `git diff --check` passed, `verify-release.ps1` passed, and `check-tracked-vs-untracked-md` passed after commit. Pushed HEAD `8fc1361` passed hosted runs: `ci` 27825503005, `cross-platform-smoke` 27825502983, and `cross-platform-source-smoke` 27825502972. TASK-0183 catalog rule id and severity stability test is next.
17. Local TASK-0183 validation on 2026-06-19: implementation commit `4aaa157` adds `tests/AgentContextKit.Tests/CatalogRuleStabilityTests.cs` with two tests: `CatalogRuleShapeMatchesStructuralInvariants` (id pattern, no duplicates, severity enum, non-empty fields) and `CatalogRuleBaselineJsonDoesNotDrift` (structural deep-equal against an embedded JSON baseline ordered by id; on drift the test writes the actual baseline to a temp path so the new baseline can be regenerated and committed alongside the intentional catalog change). `dotnet build` clean (0 warnings, 0 errors), focused tests 2/2 green, full suite 305/305 green, source `ackit scan --ci` exit 0 with existing `.remember` Medium findings only, `ackit doctor` 13/13 PASS, `git diff --check` passed, `verify-release.ps1` passed, and `check-tracked-vs-untracked-md` passed after commit. Pushed HEAD `4aaa157` passed hosted runs: `ci` 27826714694, `cross-platform-smoke` 27826714743, and `cross-platform-source-smoke` 27826714656. TASK-0184 scan `--include` / `--exclude` glob filters is next.
18. Local TASK-0184 validation on 2026-06-19: implementation commit `f5873eb` extends `IRepositoryScanner.Scan` with optional `includeGlobs`/`excludeGlobs`, adds `GlobMatcher` for `*`/`**`/`?` glob-to-regex matching, plumbs `--include` / `--exclude` (repeatable) through `ackit scan`, rejects empty/whitespace globs with a localized "Invalid argument" error and exit `1` (the task spec mentioned exit `2`, but `2` is reserved for critical risk per `docs/EXIT_CODES.md`; `1` matches the existing invalid-invocation convention used for `task` without title, unknown commands, and unhandled runtime errors), updates `CLI_CONTRACT.md`, `CLI_REFERENCE.md`, and `check-cli-contract.ps1`, adds the `invalidArgument` localization key, and adds 8 new tests in `tests/AgentContextKit.Tests/ScanIncludeExcludeTests.cs`. `dotnet build` clean (0 warnings, 0 errors), focused tests 8/8 green, full suite 313/313 green, source `ackit scan --ci` exit 0, `ackit doctor` 13/13 PASS, `check-cli-contract` and `check-localization-parity` passed, `check-tracked-vs-untracked-md` passed after commit, `git diff --check` clean, `verify-release.ps1` passed. Pushed HEAD `f5873eb` passed hosted runs: `ci` 27829523172, `cross-platform-smoke` 27829523186, and `cross-platform-source-smoke` 27829523206. TASK-0185 nightly local check workflow is next.
19. Local TASK-0185 validation on 2026-06-19: implementation commit `7311d4e` adds `.github/workflows/nightly-local-check.yml` (cron `17 5 * * *`, schedule + workflow_dispatch, ubuntu-latest + windows-2025 matrix, `contents: read` only, 14-day artifact retention), adds `NightlyWorkflowYamlGuardTests` (parses the YAML with YamlDotNet 15.1.6, asserts `schedule` and `workflow_dispatch` triggers), updates `docs/HOSTED_CHECKS.md`. `dotnet build` clean (0 warnings, 0 errors), focused tests 2/2 green, full suite 315/315 green, source `ackit scan --ci` exit 0, `ackit doctor` 13/13 PASS, `check-tracked-vs-untracked-md` passed after commit, `git diff --check` clean, `verify-release.ps1` passed. Pushed HEAD `7311d4e` passed hosted runs: `ci` 27830798357, `cross-platform-smoke` 27830798402, and `cross-platform-source-smoke` 27830798370. TASK-0186 final validation and hosted check sync is next.
20. Local TASK-0186 validation on 2026-06-19 (PROJECT-CONTROL-0109 closeout): all local gates passed — `dotnet build` clean (0 warnings, 0 errors), full suite 315/315 green, source `ackit scan --ci` exit 0, `ackit doctor` 13/13 PASS, `check-cli-contract` and `check-localization-parity` passed, `check-tracked-vs-untracked-md` clean, `git diff --check` clean, `verify-release.ps1` passed. Pushed HEAD `a1151c7` (the final evidence commit on TASK-0185) passed hosted runs: `ci` 27831342159, `cross-platform-smoke` 27831342158, and `cross-platform-source-smoke` 27831342189. PROJECT-CONTROL-0109 (TASK-0177 through TASK-0186) is closed; PROJECT-CONTROL-0110 closed at TASK-0196 (`b073c3d`) on 2026-06-19 with cumulative suite 428/428 green: MCP transport step 2 (real stdio), `ackit watch` local implementation, watch debounce/ignore-list/cancellation unit tests, trim edge case tests, `ackit.rules` MCP read-only metadata tool, localization parity for new commands and MCP step 2 error surface, docs update, docs-first audit + state sync, final validation. No version bump and `0.2.0-alpha.3` remains NO-GO.

21. Local TASK-0188 validation on 2026-06-19 (PROJECT-CONTROL-0110 step 1): planning commit `ea2f681`, implementation commit `6eb7102` adds Core `McpStdioTransport` with line-delimited JSON-RPC read/write loop, 1 MiB max line, 30 s per-request timeout, cancellation token, privacy boundary (no raw match, no absolute path in stdout), and CLI `--stdio-server` long-lived mode alongside the existing `--stdio` one-shot. `dotnet build` clean, focused tests 30/30 green, full suite 345/345 green, `ackit mcp --help`, `--stdio-server`, and the legacy `--stdio` all return exit 0. Pushed HEAD `fdecdd5` (evidence). DIAG-001 (`mcp__plugin_*` transport) remains open and is independent of the local stdio loop.
22. Local TASK-0189 validation on 2026-06-19: planning commit `b481a0e`, implementation commit `447a76a` ships the previously design-only `ackit watch` command with `Core/Watcher/{FileWatcherEvent, IFileWatcher, PhysicalFileWatcher, FakeFileWatcher, WatchDebouncer, WatchIgnoreFilter, ScanChangeReport, WatchRunner}`. The watcher is `System.IO.FileSystemWatcher`-backed, debounces by default 500 ms, ignores `.git`, `bin`, `obj`, generated `.ackit/*` outputs, editor swap files, and known generated extensions. `dotnet build` clean, focused watch tests pass, full suite 397/397 green, `ackit watch --help` exits 0, `--once` exits 0, `--debounce-ms 0` exits 1. Pushed HEAD `91794bb` (evidence).
23. Local TASK-0190 validation on 2026-06-19: planning commit `7349fc7`, implementation commit `8b6d4d5` adds 10 watch edge case tests (debouncer boundary, ignore-filter case insensitivity, watch-command disposal) using `FakeFileWatcher` and the injectable clock; no real file-system timing dependency. Full suite 407/407 green. Pushed HEAD `082d6a1` (evidence).
24. Local TASK-0191 validation on 2026-06-19: planning commit `6a11056`, implementation commit `353ea7f` extracts `TextTrimmer.Trim` to Core so the trim path is unit-testable without spawning the CLI process, and adds 15 `TextTrimmerTests` covering empty input, equal-length, header+note overflow, multi-byte UTF-8 boundary, null bytes, non-ASCII, determinism, large input, and argument validation. Full suite 422/422 green. Pushed HEAD `d07a5dc` (evidence).
25. Local TASK-0192 validation on 2026-06-19: planning commit `2effa0f`, implementation commit `02a89a2` adds the read-only `ackit.rules` MCP tool. The tool returns `RiskRuleCatalog` entries (id, name, category, defaultSeverity, description, recommendation) in ascending id order plus a server version field; never returns raw match, absolute path, or file content. Six new tests across `McpRouterTests` and `McpStdioTransportTests`. Full suite 428/428 green. Pushed HEAD `1a6aa68` (evidence).
26. Local TASK-0193 validation on 2026-06-19: planning commit `25ddbb5`, implementation commit `3302c67` adds 14 new bilingual keys to `TextProvider.Texts` for `trim`, `watch`, and `mcp` error surfaces, routes the watch startup message to `Console.Error` in `--json` mode, threads `language` through `RunMcpStdioServer`, and extends `LocalizationParityTests` to cover trim/watch/mcp. The underlying `LocalizationParityTests` matrix reports 5/5 green. Full suite remains 428/428. `scripts/check-localization-parity.ps1` returns exit 1 in this environment due to a pre-existing PowerShell strict-mode interaction with git's stderr warning; the xunit matrix passes. Pushed HEAD `292accc` (evidence).
27. Local TASK-0194 validation on 2026-06-19: planning commit `9771253`, implementation commit `d20a908` adds a clarifying paragraph to `docs/CLI_REFERENCE.md` documenting that `ackit watch --json` routes the startup line to `Console.Error` so `Console.Out` contains only valid JSON envelopes. `docs/MCP_STDIO_DESIGN.md`, `docs/WATCH_MODE.md`, `docs/CLI_REFERENCE.md`, and `docs/NEXT_TASKS.md` re-read and confirmed accurate against the current code paths. Full suite 428/428 green. Pushed HEAD `97711da` (evidence).
28. Local TASK-0195 validation on 2026-06-19: planning commit `01a241f`, implementation commit `0c60db8` refreshed the state-sync docs (`docs/NEXT_TASKS.md`, `.codex/SESSION_HANDOFF.md`, `.codex/CONTEXT_PACK.md`, `.codex/NEXT_STEPS.md`) to mark TASK-0188 through TASK-0194 complete and reference the 428/428 test count. No source or test changes. Pushed HEAD `c81ad67` (evidence). TASK-0196 final validation is next.
29. Local TASK-0196 validation on 2026-06-19 (PROJECT-CONTROL-0110 closeout): planning commit `1290b56`, closeout commit `2a60b0c`, evidence commit `b073c3d`. `dotnet build` clean (0 warnings, 0 errors), full suite 428/428 green, source `ackit scan --ci` exit 0 with existing `.remember` Medium log findings only, `ackit doctor` 13/13 PASS, `ackit mcp --help` and `ackit watch --help` exit 0, `ackit watch --once` exits 0, `ackit trim --input .gitignore --output .ackit/cache/trim-out.md --max-chars 200 --lang en` exits 0 with original 963 chars trimmed to 200. `git status` clean, `git diff --check` clean before the closeout commit. PROJECT-CONTROL-0110 is closed. Final pushed HEAD `b073c3d`. No release, no tag, no GitHub Release, no NuGet publish, no version bump; `0.2.0-alpha.3` remains NO-GO; RB-003 and RB-008 remain open and are independent of this control. No next project control has been selected; the next entry is "not yet selected / pending maintainer direction".
30. Local TASK-0197 validation on 2026-06-19: planning commit `d7aae06` adds a docs-first state cleanup task; implementation commit (this file's recorded evidence) refreshes `docs/NEXT_TASKS.md`, `.codex/SESSION_HANDOFF.md`, `.codex/CONTEXT_PACK.md`, and `.codex/NEXT_STEPS.md` so they all agree that PROJECT-CONTROL-0110 is closed, the final pushed HEAD is `b073c3d`, the cumulative suite is 428/428 green, and `0.2.0-alpha.3` remains NO-GO because RB-003 and RB-008 remain unresolved. No source code or test changes.
31. TASK-0198 started on 2026-06-20 as an independent docs-only investigation after takeover verification confirmed local `HEAD` and `origin/master` at `194480a`. Scope: record exact remaining maintainer evidence for `RB-003` (second verified human owner and notification coverage) and `RB-008` (destructive NuGet recovery authority and backup recovery owner). No blocker closure, release/tag/NuGet/version/workflow/security-setting action, or new project control is authorized.
