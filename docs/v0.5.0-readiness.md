# v0.5.0 Readiness Statement (single-lane chain, pre-release)

Branch: `release/v0.5.0` → `master` via PR #20 (draft until release).
This record states integration readiness only. No tag, no publish, no
release, no deployment has occurred (proofs below).

## Chain evidence (all on `release/v0.5.0`, all exact-head CI green)

| Task | Capability | Completion evidence |
|---|---|---|
| TASK-0078 | maintenance-aware release-state model | archived (prior lane) |
| TASK-0079 | deterministic verification state binding | archived (prior lane, PR #19) |
| TASK-0080 | verifier independence + replay/staleness hardening | archived `48a620d`, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0081 | canonical read-only status / next-actions | archived, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0082 | portable verification-bound handoff | archived, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0083 | provider-surface + integration parity | archived, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0084 | adversarial audit + link-aware write guard | archived, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0085 | positioning + reproducible demo-as-test | archived, verifier OVERALL PASS 0 blockers, PR CI 12/12 |
| TASK-0086 | this integration + readiness | this task |

## Composition (proven, not claimed)

- Status over bound state: `ackit.status.v1` embeds the 0079/0080 trust
  summary verbatim; `tests/e2e/chain-composition.test.ts` proves
  CLI ≡ SDK ≡ MCP ≡ Action byte-equality on one fixture.
- Cross-process handoff resume: export → import → resume proven in
  `tests/integration/checkpoint/handoff-cross-process.test.ts` and the
  `tests/e2e/trust-flow-demo.test.ts` second process.
- Surfaces agreeing: `tests/contract/projection-parity.test.ts`
  (CLI/SDK/MCP/Action) + allowlist-pinned SDK + 16-tool MCP contract.
- Demo green: 11 asserted stages, ~12s, in CI.
- Matrix green: `tests/security/adversarial-paths.test.ts` (1 proven gap
  fixed + regression-guarded; allow-list rejected on evidence).
- Release truth holding: source `0.5.0-dev.0`, stable `0.4.1`
  (`release-state.json`), extension coupled; asserted in-test.

## GO-gate scan (TASK-0086 acceptance)

- Missing acceptance evidence: none (every task archived with completion notes).
- Pending tests: none (skips are OS-gated symlink rows + one pre-existing
  conditional — counted, never silent).
- TODOs: none in v0.5 code (grep-verified).
- Weakened gates: none (every gate passes unweakened; contract tests added, none relaxed).
- Unresolved security findings: none open (1 proven gap fixed; hardlink-alias
  residual accepted + recorded in `docs/security/THREAT_MODEL.md`).
- Failed live verification: none (8/8 fresh verifiers PASS, 0 blockers).
- Missing CI evidence: none (every implementation commit exact-head green).

## No-publish proof (recorded at TASK-0086 gate time)

- `git tag --list "v0.5*"` → empty.
- `package.json` version → `0.5.0-dev.0` (unfinalized by test).
- `release-state.json` `publishedStable` → `0.4.1` (public truth intact).
- No npm publish, no GitHub Release, no Marketplace submission, no tag push
  in this lane (branch history: feature + closure commits only).

## What remains (release task, user-authorized)

Version finalization (`0.5.0-dev.0` → `0.5.0`), CHANGELOG entries (no
`Unreleased`/v0.5.0 content was added pre-release, per convention),
package/VSIX proof, PR ready → squash-merge → post-merge CI → tag →
OIDC publish → Marketplace → GitHub Release → hosted docs → stable
pointer → maintenance-branch simplification. None of it starts here.
