# TASK-0153 Fix Copilot Instructions Inconsistency Followup

## Purpose
Add a guard test that confirms `.github/copilot-instructions.md` matches the canonical "Commit And Push Policy" section so the file is not silently regressed, and add a tiny Turkish linter note in the same file for downstream context.

## Current State
- `.github/copilot-instructions.md` was updated in TASK-0140 and TASK-0143 to carry the canonical commit/push policy.
- No focused test currently guards that file's content against future drift.

## Scope
- Add a small xUnit test that asserts the canonical phrases exist in the file.
- Add a one-paragraph note that the file is generated once and not regenerated by `ackit generate`.

## Out Of Scope
- Changing the file's substantive content.
- Modifying the published `0.2.0-alpha.2` package.

## Affected Files
- `.github/copilot-instructions.md` (one-paragraph note only).
- `tests/AgentContextKit.Tests/` (guard test).

## Implementation
1. Add a guard test that asserts the canonical phrases exist in the file.
2. Add the one-paragraph "manual-only" note to the file.
3. Run the full test suite.

## Security/Privacy Boundary
- No credential, raw finding, or recovery secret may be printed.

## Backward Compatibility
- Pure additive documentation plus a guard test.

## Acceptance Criteria
- New test passes.
- `dotnet test` is 225+/225+ green.
- `ackit scan --ci` and `ackit doctor` clean.

## Tests
- One new xUnit test.

## Validation
- `dotnet build` clean.
- `dotnet test` green.
- `ackit scan --ci` clean.
- `ackit doctor` PASS.

## Rollback
- Revert the commit.

## Completion Evidence
Pending. Will be filled after the commit and hosted checks.

## Commit
- `docs: add copilot instructions guard and manual note`

## Push
- Normal `master` push after validation.

## Hosted Checks
- Standard 3/3 expected.
